# Third-party notices

LSW Audio Diagnostics Core v0.2.0 does not bundle third-party source code, binary libraries, SDKs, fonts, images, audio files, models, or datasets.

The core library uses only the C++ standard library supplied by the selected compiler toolchain. The optional Windows Dashboard uses the Windows SDK Win32 and GDI system APIs and does not redistribute external assets or libraries.
