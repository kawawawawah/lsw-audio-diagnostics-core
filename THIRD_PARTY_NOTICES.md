# Third-party notices

LSW Audio Diagnostics Core v0.1.0 does not bundle third-party source code, binary libraries, SDKs, fonts, images, audio files, models, or datasets.

The project uses only the C++ standard library supplied by the selected compiler toolchain.
