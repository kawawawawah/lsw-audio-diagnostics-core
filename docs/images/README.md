# Dashboard screenshot provenance

`v0.2-dashboard.png` is captured by running this repository's optional Windows Dashboard Example. The Dashboard uses deterministic synthetic signals and feeds them into the real `Analyzer<float>` implementation.

The image contains no third-party artwork, logos, or font files. The core library remains platform-independent; the Dashboard is a Windows-only optional Win32/GDI example.
